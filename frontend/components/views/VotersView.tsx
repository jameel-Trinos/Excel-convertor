"use client";

import { PlaceholderView } from "./shared/StateComponents";

export function VotersView() {
  return (
    <PlaceholderView
      title="Voters"
      description="Voter list PDF processing functionality will be implemented here. This view will handle voter registration and voter list data with a dedicated backend endpoint."
    />
  );
}
